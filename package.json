{
  "name": "walk-apple-news",
  "version": "0.0.0",
  "description": "Walk & do inline transformations of an apple news feed",
  "main": "dist/index.js",
  "scripts": {
    "test": "ava && semistandard | snazzy",
    "posttest": "readme package.json --travis > readme.md",
    "build": "rm -rf dist && mkdir -p dist && babel lib --out-dir dist",
    "watch": "rm -rf dist && mkdir -p dist && babel -w lib --out-dir dist",
    "prepublish": "npm run build"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/micnews/walk-apple-news.git"
  },
  "author": "mic.com",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/micnews/walk-apple-news/issues"
  },
  "homepage": "https://github.com/micnews/walk-apple-news#readme",
  "devDependencies": {
    "ava": "^0.14.0",
    "babel-cli": "^6.7.5",
    "babel-core": "^6.7.6",
    "babel-preset-es2015": "^6.6.0",
    "semistandard": "^7.0.5",
    "snazzy": "^3.0.0"
  }
}
